let counter = 1;
let body = document.querySelector('body');
const clickFunc = () => {
    // body.classList.add('bgColor');
    // body.classList.remove('bgColor');

    body.classList.toggle('bgColor');

    // if have class remove it else if don't have class , set it
}